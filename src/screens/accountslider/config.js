import { Dimensions } from 'react-native'

const { width: screenWidth } = Dimensions.get('window')

export const cardPerSlide = 3
export const cardPadding = 0
export const paddingAround = 0// slide horizontal padding
export const cardBetweenPadding = 0
export const totalPadding = 0
export const imageWidth = 60
export const imageHeight = 60